version: '3.8'

services:
  evolution-api:
    image: evoapi/evolution-api:v2.1.2
    container_name: evolution_api
    restart: always
    ports:
      - "8080:8080"
    environment:
      - SERVER_URL=http://localhost:8080
      - AUTHENTICATION_API_KEY=RosymarSecretKey2026
    volumes:
      - evolution_instances:/evolution/instances

volumes:
  evolution_instances:
